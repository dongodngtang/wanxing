{
  "name": "root",
  "private": true,
  "scripts": {
    "start":"lerna exec yarn start --scope=app",
    "ele":"lerna exec yarn start --scope=electron"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
